export PYBUILD_NAME=paramiko
export PYBUILD_TEST_ARGS='{interpreter} python-paramiko/test.py'

%:
	dh $@ --with python2 --buildsystem=pybuild

override_dh_auto_test:
	PYBUILD_SYSTEM=custom dh_auto_test


